const array = [70, 70, 80]

console.log(mediadonumero(array))

function media(numeros) {
    const soma = 0
    for (let numero of numeros) {
        soma += numero
    }
    const media = soma / (notas.lenght);
}